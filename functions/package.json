{
  "name": "securedrop-functions",
  "description": "Cloud Functions for SecureDrop audit logs",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^4.9.0",
    "secure-drop": "file:.."
  }
}
