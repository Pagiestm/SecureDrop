{
  "name": "securedrop-functions",
  "description": "Cloud Functions for SecureDrop audit logs",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "scripts": {
    "serve:functions": "firebase emulators:start --only functions",
    "serve": "npm run serve:functions",
    "shell": "firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5",
    "secure-drop": "file:.."
  }
}
